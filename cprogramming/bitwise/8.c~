// swap 2 no without using temp. use bitwise operator

#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter the 2 no to swap\n");
	scanf("%d%d",&a,&b);
	a = a^b;
	b = a^b;
	a = a^b;
	printf("swaped no = %d\t %d",a,b);
	return 0;
}
