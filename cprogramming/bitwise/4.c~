// Count no of bit set in no

#include<stdio.h>
int main()
{
	int a,i,count=0;
	printf("Enter a no\n");
	scanf("%d",&a);
	for(i=0;i<31;i++)
	{

		 if ((a & (1<<i)) != 0) 
				count = count + 1;
			
	}
	printf("Bit set count in %d is %d\n",a,count);
	return 0;
}
