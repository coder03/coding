// swap the nibbles of a no

#include<stdio.h>
int main()
{
	int x,result;
	printf("Enter the no to swap the nibbles of the no\n");
	scanf("%d",&x);
	result = ((x>>4) & 0x0f) | ((x<<4) & 0xf0);
	printf("Nibble exchanged = %d\n",result);
	return 0;
}
