/* write version of itoa that accepts 3 arguments instead of two, the 3rd 
argument is minimum field width. the conversed no must be padded with 
blanks on the left if necessary.

Example : itoa 2 5 =====> output:   10; ie, 3 blanks folled by 10
*/

#include<stdio.h>
int main()
{
	int i
	char ch;
	
